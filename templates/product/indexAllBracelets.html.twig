{# VERSION EN BLOC #}
{% extends 'base.html.twig' %}

{% block title %}BRACELETS{% endblock %}

{% block body %}
<section class="header_product">
        <div class="text-box">
            <h1>BRACELETS</h1></br></br>
            <a href="#" class="hero-btn">Venez découvrir notre marque</a>
        </div>
</section>

    <div class="container">
       <section class="product-list">
                    {% for brac in bracelet %}
                    <div class="card">
                        <div class="image"> <img src="{{asset("uploads/" ~ brac.photo)}}" alt="bijoux"> </div> 
                        <div class="title">{{brac.name}}</div>
                        <div class="text">{{brac.unitprice}}€</div>
                        {# <a class="buy-now" href="#">Ajouter au panier</a> #}
                        <a class="buy-now" href="{{path('earing', {'id': brac.id} )}}">Voir produit</a>
                    </div>


                    {% endfor %}
        </section>

    </div>
















{# TEST POUR FAIRE APPARATITRE TOUS LES PRODUITS DANS UNE SEULE FONCITON ET PAGE #}
    {# <div class="container">
       <section class="product-list">
       <pre>
           {{ dump(subCate) }}
       </pre>
                    
                    <div class="card">
                        <div class="image"> <img src="https://i.etsystatic.com/34806363/r/il/9008d3/3879418349/il_1588xN.3879418349_omhf.jpg" alt="boucle d'oreille"> </div>
                        <div class="title">{{bracelet.name}}</div>
                        <div class="text">{{bracelet.unitprice}}</div>
                        <a class="buy-now" href="#">Ajouter au panier</a>
                         <a class="buy-now" href="#">Voir produit</a>
                    </div>
                   
        </section>
    </div> #}
{% endblock %}

 
        
    
{# VERSION EN TABLEAU #}
{# 

{% extends 'base.html.twig' %}

{% block title %}BRACELETS{% endblock %}

{% block body %}
<h1>BRACELETS</h1>

    <div class="container">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th class="text-center" scope="col">Nom</th>
                    <th class="text-center" scope="col">Description</th>
                    <th class="text-center" scope="col">Quantité</th>
                    <th class="text-center" scope="col">Prix unitaire</th>
                </tr>
            </thead>
            <tbody>
                    {% for brac in bracelet %}
                        <tr>
                            <td class="text-center" scope="col">{{brac.name}}</td>
                            <td class="text-center" scope="col">{{brac.description}}</td>
                            <td class="text-center" scope="col">{{brac.quantity}}</td>
                            <td class="text-center" scope="col">{{brac.unitprice}}</td> 
                        </tr>
                    {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %} #}