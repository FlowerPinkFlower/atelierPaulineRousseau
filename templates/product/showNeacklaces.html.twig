{# VERSION TABLEAU #}
{% extends 'base.html.twig' %}

{% block title %}COLLIERS{% endblock %}

{% block body %}
<h1>COLLIER</h1>

    <div class="container">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th class="text-center" scope="col">Nom</th>
                    <th class="text-center" scope="col">Description</th>
                    <th class="text-center" scope="col">Quantité</th>
                    <th class="text-center" scope="col">Prix unitaire</th>
                </tr>
            </thead>
            <tbody>
                    
                    <tr>
                        <td class="text-center" scope="col">{{necklace.name}}</td>
                        <td class="text-center" scope="col">{{necklace.description}}</td>
                        <td class="text-center" scope="col">{{necklace.quantity}}</td>
                        <td class="text-center" scope="col">{{necklace.unitprice}}</td> 
                    </tr>
                    
            </tbody>
        </table>
    </div>
{% endblock %}